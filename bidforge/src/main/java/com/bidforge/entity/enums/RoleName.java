package com.bidforge.entity.enums;


public enum RoleName {

    // Default role that every registered account gets
    ROLE_USER,

    // can manage users, view everything, cancel auctions, read audit log, ...
    ROLE_ADMIN
}
